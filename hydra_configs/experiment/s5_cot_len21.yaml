# @package _global_
defaults:
  - /pipeline: pretrain
  - /task: s5_cot_len21
  - /model: s5_len21_cot
  - /optim: synthetic_expert
  - /runtime: gpu_float16
  - /logging: disabled
  - /cluster: local
  - _self_

run:
  name: s5-cot-len21-depth1-v2
  out_dir: ${run.output_root}/out-s5-cot-len21-depth1-v2

runtime:
  compile: true

optim:
  s5_eval_metrics: true
