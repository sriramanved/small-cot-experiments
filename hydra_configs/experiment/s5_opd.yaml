# @package _global_
defaults:
  - /pipeline: opd
  - /task: s5_opd
  - /model: teacher_inferred
  - /optim: opd
  - /runtime: gpu_float16
  - /logging: enabled
  - /cluster: local
  - _self_

run:
  output_root: ${path_join:${s5_suite_root:${task.s5_m},${task.teacher_seed},${task.render_seed},${optim.seed}},opd_${task.objective}}
  name: ${s5_opd_run_name:${task.objective},${task.s5_m},${task.subset_size},${task.eta},${task.teacher_law},${task.student_rollout_temperature},${task.student_temperature},${optim.seed},nanogpt}
  out_dir: ${path_join:${run.output_root},${out_name:${run.name}}}
