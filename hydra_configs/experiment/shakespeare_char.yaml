# @package _global_
defaults:
  - /pipeline: pretrain
  - /task: shakespeare_char
  - /model: shakespeare_char
  - /optim: shakespeare_char
  - /runtime: gpu_float16
  - /logging: disabled
  - /cluster: local
  - _self_

run:
  name: mini-gpt
  out_dir: ${run.output_root}/out-shakespeare-char

logging:
  wandb_project: shakespeare-char
