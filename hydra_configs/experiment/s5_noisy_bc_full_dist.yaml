# @package _global_
defaults:
  - /pipeline: pretrain
  - /task: s5_noisy_full_dist
  - /model: s5_len21_cot
  - /optim: synthetic_offline_bc
  - /runtime: gpu_float16
  - /logging: enabled
  - /cluster: local
  - _self_

run:
  name: s5-noisy-bc-full-dist-sample-then-corrupt-n${task.subset_size}-eta${float_tag:${task.eta}}
  out_dir: ${run.output_root}/out-s5-noisy-bc-full-dist-sample-then-corrupt-n${task.subset_size}-eta${float_tag:${task.eta}}

optim:
  offline_target_type: teacher_probs
  s5_eval_metrics: true
  s5_eval_clean_train_loss: true
