# @package _global_
task:
  dataset_prefix: s5_noisy_offline
  subset_size: 1000000
  eta: 0.2
  dataset: ${task.dataset_prefix}_n${task.subset_size}_eta_${float_tag:${task.eta}}
  s5_mode: cot
  s5_m: 21
  rollout_mode: greedy_then_corrupt
  target_mode: tokens
