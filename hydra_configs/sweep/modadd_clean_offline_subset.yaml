# @package _global_
sweep:
  name: modadd_clean_offline_subset

run:
  name: modadd-clean-offline-bc-p${task.modadd_p}-m${task.modadd_m}-n${optim.offline_train_subset_size}
  out_dir: ${run.output_root}/out-modadd-clean-offline-bc-p${task.modadd_p}-m${task.modadd_m}-n${optim.offline_train_subset_size}

hydra:
  sweeper:
    params:
      optim.offline_train_subset_size: 250000,500000,1000000,2000000,4000000,6000000
